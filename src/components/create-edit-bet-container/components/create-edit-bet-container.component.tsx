import type { FunctionComponent } from "react";

import z from 'zod';

interface OwnProps {
    handleSubmit: (e: React.FormEvent<HTMLFormElement>) => void;
}

const Component: FunctionComponent<OwnProps> = ({}) => {
  
  return <div></div>;
}

export default Component;